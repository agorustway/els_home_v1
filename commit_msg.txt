fix: 계정 저장/복원 로직 강화 — Supabase 실패 시 sessionStorage 백업

[문제]
- 계정 저장 후 다른 페이지 갔다 오면 계정 정보가 풀림
- upsert가 UNIQUE 제약 없이 실패 가능성 + 에러 무시 구조

[수정]
- handleSaveCreds: sessionStorage 백업 추가 + 에러 로그 출력
- handleLogin: await handleSaveCreds로 저장 완료 보장
- init: API 빈 값 시 sessionStorage 백업에서 복원하는 2차 로직 추가
- POST API: upsert → select→update/insert 패턴으로 교체 (UNIQUE 없어도 동작)
