# 🚩 ELS MISSION CONTROL (v5.12.18 / APK v5.11.10)

> **"아산 PWA 모바일 저장일시 레이아웃 안정화 (v5.12.18)"**
> - **최신 업데이트**: 아산 배차판/선적관리 모바일 화면에서 `저장일시 + 경과시간`이 상단 메뉴를 밀어 화면 구성이 깨지던 문제를 모바일 전용 CSS로 보정했습니다. PC 레이아웃은 유지했습니다.

### 🚀 현재 상태 (CURRENT STATUS)
- **버전**: v5.12.18
- **주요 사항**:
  - **[아산 배차판 소수 수량 집계]**: 글로비스 KD 외 자료의 `0.9`, `0.1` 같은 분할 오더가 `parseInt`로 0 처리되어 오늘 웹 합계가 66건이 아닌 65건으로 보이던 문제를 수정. 배차판/종합현황/AI RAG 모두 소수 수량을 정확히 합산.
  - **[아산 PWA 모바일 UI]**: 배차판/선적관리의 저장일시 영역을 모바일에서 말줄임+경과 배지 구조로 정리해 상단 메뉴 및 탭이 밀리지 않도록 보정.
  - **[AI 배차 통계 무결성]**: 서버에서 요약문을 만들 때 화주별(글로비스/모비스) 합산이 누락되지 않도록 로직 보완. AI는 이제 직접 계산할 필요 없이 서버가 준 최종 수치만 읽으면 됨.
  - **[GPS 신뢰도]**: 앱 포그라운드 복귀 시 불필요한 GPS 재기동 제거 및 필터 임계값 조정을 통한 경로 정밀도 향상.
  - **[배차 RAG 고도화]**: 아산 배차판의 오더(계) vs 실제 배차수량 구분, 지역 컬럼(부산/인천 등) 파싱, 셀 메모 시간 해석 규칙을 AI 지시사항에 완벽 반영.
  - **[AI 배차 분석]**: 날짜별 소계(byDate) 집계 추가로 "어제/오늘/특정일" 배차 대수를 명확히 구분하여 답변.
  - **[AI 챗봇 고도화]**: 차량 출발지/도착지 주소 추적 및 내일 기상 예보 데이터 주입. Omni-Intelligence 지침 적용으로 봇의 기술적 추론 및 통찰 제시 능력 강화.
  - **[웹 관제 UX 개선]**: 전체화면 시 '전체화면 해제' 및 '포커스 맞추기' 버튼을 중앙 필터 바 내부로 통합하여 버튼 겹침 및 레이아웃 붕괴 해결.
  - **[네비게이션 모드]**: 앱 지도 화면에서 1초 단위 전경 GPS 샘플을 받아 내 차량 마커를 부드럽게 이동. 서버 전송은 기존 필수 포인트 필터 유지.
  - **[앱 지도 제어]**: 중복 버튼 통합(📍 내 위치), 마커 클릭 시 상세 경로 정보 패널 노출. 자동 추적 일시 중지 로직 포함.
  - **[드라이버 앱 작업지 입력]**: 운행 시작/일지 수정 화면의 작업지 필드는 상세조회 없이 자유입력만 허용.
  - **[앱 애니메이션]**: GPS 갱신 시 마커가 부드럽게 활주하는 `animateMarker` 적용
  - **[UI 정밀 조정]**: 차량 상세 모달(Full-width), 활동 로그(200px), 운전원 관리(nowrap) 최적화 완료.
  - **[GPS 신뢰도]**: 지하주차장 종료 시 GPS 타임아웃 대응 (고정밀 4초 -> 저정밀 2초 재시도 -> 최종 fallback 강제 기록).
  - **[필터 보정]**: `marker_type`이 포함된 포인트는 이상치 필터(`trimEndpointOutliers`)에서 제외하여 경로 보존.
- **[드라이버 앱 지도 운행 보호]**: 같은 차량의 완료 운행이 현재 운행보다 최신으로 대표 선택되는 문제를 차단하고, 앱 지도 기본 목록/마커에서 완료 운행을 제외해 경로 상세 조회 시 현재 운행 오인을 방지.


## 🕒 실시간 가동 지표 (Automation)
| 타겟 | 주기 | 상세 로직 | 상태 |
| :--- | :--- | :--- | :--- |
| **아산 배차 (Fast)** | 실시간 (변경 시) | 엑셀 저장 시 **오늘 기준 ±15일** 즉시 업데이트 (Fast Sync) | ✅ 가동 중 |
| **아산 배차 (Slow)** | 10분 주기 | 변경 여부와 관계없이 **전체 시트** 순차 업데이트 (Slow Sync) | ✅ 가동 중 |
| **아산 선적관리** | 실시간 (조회 시) | 엑셀 저장 시 **mtime 감지** 즉시 파싱 (인메모리 캐시) | ✅ 가동 중 |

| **안전운임/차량관제** | 실시간 | ETRANS 및 위치 데이터 연동 | ✅ 가동 중 |

### 🚀 마일스톤 (Milestones)
- [✅] **Phase 1-5**: AI 에이전트 및 RAG 엔진 구축 완료
- [✅] **Phase 6**: AI 지능 고도화 및 영구 학습 체계 구축
- [✅] **v5.1**: K-SKILL 구조조정 + AI 가이드 재설계
- [✅] **v5.5**: 아산 배차판 완전 자동화 및 듀얼 동기화 (v5.5.13) ✅
- [✅] **v5.10**: 차량위치관제(GPS) 리팩토링 및 안전운임 거리 기준 보강 (v5.10.35) ✅
- [✅] **v5.12**: 아산지점 선적관리 탭 및 종합상황판 개편 완료 ✅
- [ ] **Next**: 사용자별 접근 권한 분리 및 최종 인트라넷 이관

- [✅] **[FEAT] 차량관제 네비게이션 모드 및 UI 안정화**: 앱 내 지도 조작 UI 직관화 및 마커 클릭 시 상세 경로 표시. 실시간 추적 시 지도가 차량을 부드럽게 따라가며, 마커 이동 애니메이션과 UI 레이아웃 최적화를 통해 관제 경험을 완성했습니다 (v5.11.4).
- [✅] **[FEAT] 차량관제 시스템 중앙 집중화 및 맵 UX 고도화**: 기사 앱에서의 지도 공개 여부 설정을 차단하고 관제 시스템(웹)에 권한을 집중했습니다. (v5.11.2).
- [✅] **[FEAT] NAS 백엔드 벡터화 중단 및 RAG 정리**: (v5.11.1).

## 🎯 현재 목표: Omni-Agent 완성 및 마이그레이션 착수
1.  **[DONE]** K-SKILL 403 차단 회피를 위한 NAS 백엔드 프록시 구축 완료
2.  **[DONE]** RAG 시맨틱 검색(`match_documents`) 유실 복구 및 대기료 할루시네이션 해결
13. **[DONE]** NAS 문서 크롤러 및 벡터화 (Phase 5) 완료 — **AI 에이전트 개발 100% 종료**
14. **[TODO]** 법인/계정 이관 및 최종 마이그레이션 (Phase 6: Rollback/Cleanup)
15. **[DONE]** 차량 관제 네비게이션 모드 및 부드러운 마커 애니메이션 필드 적용

## 📦 최신 배포 정보
- **v5.11.4**: 앱 내 지도 버튼 통합 및 마커 클릭 액션 변경 패치.
- **다음 단계**: 운영 DB 마이그레이션 적용, NAS core 배포, 드라이버 APK 빌드.

## ⚠️ APK 빌드 절차 (필독)
```
# ✅ 올바른 APK 빌드
powershell -ExecutionPolicy Bypass -File scripts\build_driver_apk.ps1
# ❌ 절대 금지
npx cap sync   ← 단독 실행 금지
```
드라이버 앱 소스 = `web/driver-src/` | 버전 소스 = `build.gradle` 단일 진실

---
*최종 갱신일: 2026-05-12 (by Codex | v5.12.18 Asan Mobile Save-Time Layout Fix)*
