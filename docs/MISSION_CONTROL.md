# 🧠 ELS 프로젝트 현황판 (MISSION CONTROL)
#
# ╔══════════════════════════════════════════════════════════════════╗
# ║  이 파일은 프로젝트의 "작업 기억(Working Memory)"이다.          ║
# ║  모든 AI 에이전트(Antigravity, Gemini, Cursor 등)는            ║
# ║  대화 시작 시 이 파일을 반드시 먼저 읽어야 한다.                ║
# ║                                                                ║
# ║  📏 규칙: 이 파일은 100줄 이내로 유지한다.                      ║
# ║  상세 내용은 DEV_LOG.md, ARCHITECTURE.md 등에 기록한다.          ║
# ╚══════════════════════════════════════════════════════════════════╝
#
# 마지막 업데이트: 2026-03-09
# 업데이트한 사람: Antigravity Agent (안전운임 구간/경로 조회 UI/UX 전면 고도화)

---

## 1. 프로젝트 정체성

| 항목 | 내용 |
|------|------|
| **프로젝트명** | ELS Solution - 임직원 포털 & 인트라넷 |
| **도메인** | nollae.com (Vercel 배포) |
| **소유자** | 형 (hoon, 42세) — AI-인간 협업 방식으로 개발 |
| **AI 페르소나** | 똑 부러지는 남동생 — 아닌 건 아니라고 말하는 FM 스타일 |
| **의사소통** | 형 7 : AI 3 — 사실 기반 제안 및 상호 협력 필수 |
| **목적** | 물류 회사 임직원의 업무 효율화 (컨테이너 조회, 업무보고, 자료실 등) |

---

## 2. 기술 스택 요약

| 계층 | 기술 | 위치 |
|------|------|------|
| **프론트엔드** | Next.js 14 + React | `web/` → Vercel 배포 |
| **인증/DB** | Supabase (Google/Kakao/Naver OAuth) | 클라우드 |
| **파일 저장** | MinIO (S3 호환) on NAS | `elssolution.synology.me` |
| **ELS 백엔드** | Flask (Python) | `docker/els-backend/` → NAS Docker |
| **ELS 봇 엔진** | Selenium + Chrome | `elsbot/` → NAS Docker |
| **데스크탑 앱** | Electron | `desktop/` (개발 중) |

---

## 3. 핵심 서비스 포트

| 서비스 | 로컬 포트 | 배포 주소 |
|--------|-----------|-----------|
| 프론트엔드 (Next.js) | `3000` | nollae.com |
| ELS 백엔드 (Flask) | `2929` | `elssolution.synology.me:8443` |
| ELS 데몬 (Selenium) | `31999` | NAS Docker 내부 |

---

## 4. 현재 상태 (TODO)

### 🔴 진행 중
- (수정 완료 — 운영 환경 배포 및 모니터링 대기)

### 🟡 알려진 이슈
- NAS CPU 부하 시 Selenium 타임아웃 증가 경향 (세션 2개로 제한 및 간격 60초 연장 조치 완료)
- 봇 테스트 빈도 주의 (ETRANS 측 봇 탐지 가능성)
- `.env.local`에 사용하지 않는 키 잔존 (정리 필요)

### 🟢 최근 완료 (최신 5건)
- [2026-03-12] **중부지점 페이지 구글 스프레드시트 연동**: 사용자의 요청에 따라 중부지점 전용 공간을 구글 스프레드시트 임베드 화면으로 대체. 여백을 최소화하여 인트라넷 내에서 즉시 작업 및 조회가 가능하도록 구현.
- [2026-03-12] **업무보고 디자인 및 편의성 개선**: 목록, 상세, 작성 3개 페이지의 버튼 디자인을 32px 높이로 통일하고 일관된 스타일 적용. 본문 하단 작성자 정보 레이블 수정(성함 → 이름), 지점명 한글 매핑 저장, 작성 시 사용자 소속 지점 자동 선택 기능 구현.
- [2026-03-12] **안전운임 조회 상하차지 동일 검색 오류 해결**: 안전운임 구간/경로 조회 시 출발지와 도착지가 동일(0km)할 경우 발생하던 검증 오류와 API 호출 예외를 수정. 동일 위치 시 가상 0km 결과를 생성하여 최소 거리 운임(1km) 또는 해당 구간 운임 조회가 가능하도록 개선.
- [2026-03-11] **엑셀 다운로드 전역 틀고정 규격 최적화**: `xlsx` 기준 `!views` 속성(`ySplit` 및 `topLeftCell`) 적용하여 1행/3행 등 다중행 고정 시 엑셀에서 정확히 동작하도록 컨테이너 이력조회 및 전체엑셀다운로드 등 이슈 수정 완료.
- [2026-03-11] **문의하기 페이지 UI/UX 전면 개편**: 글래스모피즘 테마 적용, 불필요한 이모지 제거 및 모바일 최적화 고도화.
- [2026-03-11] **연락처 메뉴 개편 및 외부연락처 확장**: '작업지안내'를 '고객사정보'로 변경, 사이드바 순서 재정렬 및 일괄 삭제 기능 구현.
- [2026-03-11] **엑셀 다운로드 표준 수립 및 전역 적용**: `DESIGN_SYSTEM.md`에 규격 수립(맑은 고딕, 10pt, 제목열 회색, 틀고정, 필터, 자동 너비). 아산지점(통합현황 포함) 및 컨테이너 이력조회에 신규 규격 적용.
- [2026-03-09] **아산지점 모비스AS 데이터 유실 해결**: '국가명' 헤더 정규화('국가') 및 통합현황 매핑 수정으로 데이터 정상 노출 완료.
- [2026-03-09] **자료실 다운로드 로직 일원화 및 호환성 강화**: 업무자료실/서식자료실 UI 통일, NAS 파일 프록시 다운로드 구현, `Content-Disposition` RFC 5987 대응(한글 파일명 깨짐 방지).
- [2026-03-02] 모바일 자유게시판/자료실/연락처/업무보고/관리페이지 아산 배차판 기준 고밀도 버튼/헤더 일괄 적용 (가로 배열 통일, 버튼 32px 축소)
- [2026-03-02] 앱 스플래시(PWA) 로딩 화면 검은색 배경색 적용(`manifest.json` 수정) 및 데스크탑 스플래시 화면 미출력(1024px 이상) 분기 처리
- [2026-03-02] 관리자(권한/로그/대시보드) 및 인트라넷 각종 페이지 데스크탑 가로 폭 무제한 확장 및 컨트롤 버튼 구조 통일
- [2026-03-01] 아산 배차판 기본 뷰 통합현황으로 변경, 데이터 있는 날짜 자동 선택, 컬럼 패널 바텀시트 전환

---

## 5. 파일 시스템 지도

```
els_home_v1/
├── docs/                   ← 🧠 AI 통합 메모리 (이 파일이 여기 있음)
│   ├── MISSION_CONTROL.md  ← 현황판 (지금 읽고 있는 것)
│   ├── RULES.md            ← 협업 규칙
│   ├── DEV_LOG.md          ← 개발 이력 (통합본)
│   ├── ARCHITECTURE.md     ← 시스템 아키텍처 상세
│   ├── RUNBOOK.md          ← 운영/배포/트러블슈팅 가이드
│   └── _archive/           ← 통합 전 기존 파일 백업 (gitignore)
├── scripts/                ← 운영 스크립트 (ps1, bat, sh)
├── .tmp_test/              ← 🧪 테스트 임시 공간 (자동 생성/삭제, gitignore)
├── .tmp_issues/            ← 🐛 미해결 이슈 보관 (해결 시 삭제, gitignore)
├── web/                    ← Next.js 프론트엔드
├── elsbot/                 ← Selenium 봇 엔진
├── docker/els-backend/     ← Flask 백엔드
├── desktop/                ← Electron 데스크탑 앱
└── work-docs/              ← 업무 참고 자료 (PDF, 엑셀 등)
```

---

## 6. 긴급 연락처 (AI가 알아야 할 것)

- **주 언어**: **한글** (UTF-8, BOM 없음)
- **Git 커밋**: 한글 메시지 필수, `git commit -F commit_msg.txt` 방식
- **호칭**: 사용자를 **'형'**이라 부름
- **Push 정책**: 형이 명시적으로 요청할 때만 push
- **테스트**: `.tmp_test/`에서만 수행, 완료 후 삭제
- **상세 규칙**: `docs/RULES.md` 참조
