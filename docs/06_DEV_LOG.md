# 📖 개발 이력 (DEV LOG)
#
# ╔══════════════════════════════════════════════════════════════════╗
# ║  이 파일은 프로젝트의 "장기 기억(Long-term Memory)"이다.        ║
# ║  모든 개발 이력을 시간순으로 기록한다.                          ║
# ║                                                                ║
# ║  📏 규칙:                                                      ║
# ║  - 최신 항목이 맨 위                                           ║
# ║  - 최근 5건은 상세하게, 그 이전은 1줄 요약                     ║
# ║  - 새 작업 완료 시 여기에 추가                                  ║
# ║                                                                ║
# ║  🔗 통합 출처:                                                  ║
# ║  - /DEVELOPMENT_LOG.md (루트)                                   ║
# ║  - /web/DEVELOPMENT_LOG.md                                      ║
# ║  - /ELS_FIX_REPORT.md                                          ║
# ╚══════════════════════════════════════════════════════════════════╝
#
# 마지막 업데이트: 2026-03-22
# 업데이트한 사람: Antigravity Agent (기사 정보 DB 연동 성공 및 네이티브 설정/업로드 이슈 진단)

## 📅 2026-03-22: 인트라넷 드라이버 DB 연동 성공 및 앱 권한/통신 이슈 정밀 진단
### 핵심 성과
- **인트라넷 드라이버 정보(Supabase) 실시간 연동 성공**:
  - 기존 앱에서 '등록된 정보가 없습니다'라고 뜨던 근본 원인이 **Supabase RLS(Row Level Security)** 보안 정책에 의한 `Anon` 키 차단임을 확인.
  - 백엔드 API(`route.js`)에 `createAdminClient`를 도입하여 `Service Role` 권한으로 DB를 조회하도록 수정함으로써, **인트라넷 '운전원정보' 페이지와 앱 간의 데이터 100% 동기화** 달성. (최병훈 기사님 등 기존 데이터 정상 호출 확인)
- **전화번호 매칭 로직 고도화 및 하이픈 프리(`Hyphen-free`) 검색**:
  - 사용자가 하이픈(`-`) 없이 번호를 입력하더라도 DB의 다양한 포맷(010-1234..., 0101234...)과 매칭될 수 있도록 뒤 8자리를 분리하여 `ilike` 패턴 매칭(`%part1%part2%`) 적용.
- **네이티브 권한 설정창 이동 로직 보강 (진행 중)**:
  - 안드로이드 최신 버전(14~16) 및 삼성 갤럭시 기기에서 `Settings.canDrawOverlays` 체크 후 설정창 이동이 차단되는 현상을 해결하기 위해 `getActivity().startActivity` 및 `NEW_TASK` 플래그를 Java 플러그인에 적용.
- **네이티브 통신 안정화 (safeJson 도입)**:
  - CapacitorHttp가 활성화된 상태에서 `window.fetch` 응답을 처리할 때 발생할 수 있는 JSON 파싱 오류(이미 객체인 경우 등)를 방지하기 위해 `safeJson` 헬퍼 함수를 프론트엔드 전역에 도입.

### 잔존 이슈 (Next Action)
- **Overlay 설정창 팝업 불가**: `OverlayPlugin.java` 수정에도 불구하고 특정 기기에서 시스템 설정창으로의 이동이 여전히 동작하지 않음. Intent URI 포맷(`package:`) 재검토 필요.
- **사진 업로드 및 운송 시작(POST) 실패**: `CapacitorHttp` 인터셉터와 `MULTIPART/FORM-DATA` 간의 충돌로 추정되는 업로드 오류 및 서버 연결 오류 발생. 통신 스코프 조정 예정.

## 📅 2026-03-20: 안전운임 거리 매칭 로직 교정 및 이력조회 동시성 개선
### 핵심 성과
- **안전운임(거리별) 매칭 로직 고시 준수 최적화**:
  - **거리 매칭 정상화**: 네이버 지도 실거리를 임의로 절반으로 나누어 매칭하던 수식을 제거하고, 고시(별표 5) 기준에 따라 **편도 실거리를 그대로 운임표에 매칭**하도록 수정하여 정확한 왕복 운임이 산출되도록 보완.
  - **왕복/편도 병행 표시 UX**: 사용자가 왕복 모드 또는 편도 모드를 선택함에 따라 주 금액은 크게 표시하되, 반대 모드 금액을 괄호 `(...)` 안에 자동으로 계산하여 상시 노출함으로써 업무 편의성 강화.
- **컨테이너 이력조회 동시 접속 및 좀비 락(Lock) 차단**:
  - **백엔드 진행률 관리 강화**: 조회 로직을 `try...finally` 블록으로 보호하여 예외 발생 시에도 `is_running` 상태가 반드시 해제되도록 구현.
  - **좀비 상태 자동 복구(Stale Lock Detection)**: 조회가 시작된 지 20분이 초과된 작업은 비정상으로 간주하여 백엔드가 스스로 잠금을 해제하고 다음 사용자의 조회를 허용하는 자가 치유 기능 추가.
  - **리셋 연동**: '데몬 리셋' 시 브라우저 세션뿐만 아니라 백엔드의 전역 진행률 락도 즉시 초기화되도록 연결하여 교착 상태 완벽 방지.
## 📅 2026-03-19: 운전원 앱 PWA 설치 오류 및 사진 업로드 버그 수정
### 핵심 성과
- **PWA 설치(앱 다운로드) 기능 정상화**:
  - **매니페스트 연동**: 운전원 앱 전용 레이아웃에 `manifest_driver.json` 링크가 누락되어 브라우저가 PWA로 인식하지 못하던 문제 해결.
  - **Service Worker 등록**: `sw.js` 등록 로직을 명시적으로 추가하여 PWA 설치 필수 요건 충족.
  - **설치 유도 인터랙션**: 브라우저의 `beforeinstallprompt` 이벤트를 캡처하여 '앱 다운로드' 버튼 클릭 시 실제 설치 팝업이 뜨도록 구현. iOS 사용자를 위한 수동 설치 안내 메시지 보강.
- **사진 업로드 정합성 및 예외 처리 강화**:
  - **운행 시작 시점 업로드 유실 방지**: 운행 시작 전 미리 등록한 사진이 서버로 전송되지 않던 로직을 수정하여, `handleStart` 성공 직후 로컬 사진이 있을 경우 즉시 서버(S3/NAS)로 일괄 업로드하도록 보완.
  - **상세 에러 피드백**: 사진 전송 실패 시 단순 알럿 대신 서버에서 반환한 구체적인 에러 메시지(`alert`)를 표시하여 사용자 디버깅 지원.

## 📅 2026-03-19: 운전원 앱 모바일 최적화 및 접근성 강화
### 핵심 성과
- **운전원 앱(`driver-app`) 모바일 한 화면 최적화**:
  - **컴팩트 레이아웃**: 입력 필드 높이 축소(52px → 44px) 및 여백 조정을 통해 스마트폰 한 화면에 '운행 시작' 버튼까지 노출되도록 개선.
  - **입력 폼 재배치**: 이름/전화번호, 씰넘버/타입을 1줄에 배치(Flex)하여 세로 길이를 획기적으로 단축. 특히 컨테이너 타입은 선택이 잦은 **40FT/20FT** 중심의 드롭다운 방식으로 변경.
  - **컴포넌트 비율 최적화**: 이름 칸은 좁게(30%), 전화번호 칸은 넓게 배분하여 실제 데이터 길이에 최적화된 시각적 균형 확보.
- **실시간 운행 정보 접근성 고도화**:
  - **플로팅 타이머(Internal)**: 앱 내에서 스크롤을 내리더라도 현재 운행 시간이 우측 하단에 항상 떠 있도록 플로팅 버튼 구현. 클릭 시 최상단으로 부드러운 스크롤 이동 지원.
  - **백그라운드 타이머(System)**: Media Session API를 통해 앱 이탈 시에도 시스템 알림바 및 잠금화면에서 초 단위 실시간 운행 상태 확인 가능.
- **랜딩 페이지(`Intro`) 앱 연동 로직 개선**:
  - **원클릭 앱 실행/다운로드**: 메인 소개 페이지의 다운로드 버튼 클릭 시 모바일 기기에 따라 최적의 경로(안드로이드: 크롬 강제 실행 및 PWA 유도, iOS: 직접 접속)로 안내.
  - **다중 경로 지원**: '다운로드 버튼'은 앱 설치/실행에 집중하고, 하단 '접속주소'는 텍스트가 아닌 실제 클릭 가능한 링크로 변경하여 웹 브라우저를 통한 즉시 접속 편의성 제공.

## 📅 2026-03-19: 운전원 정보 관리 체계 효율화 및 UI/UX 고도화
### 핵심 성과
- **운전원 상세/등록/수정 필드 최적화**:
  - **중복 필드 제거**: 차량번호/차량ID와 역할이 겹치는 **'영업넘버' 및 '아이디'** 필드를 UI에서 전격 삭제(상세, 목록, 등록, 수정 페이지 전체 적용)하여 데이터 정합성 혼선 방지 및 입력 피로도 감소.
  - **소속지점 선택 방식 개선**: 자유 입력 방식의 소속지점 필드를 **시스템 표준 지점 목록(아산, 중부, 예산, 당진, 부산, 상암)** 기반의 드롭다운(Select) 방식으로 변경하여 데이터 일관성 확보.
- **편집 및 등록 UI/UX 고도화 (Premium Look)**:
  - **파일 업로드 섹션 리뉴얼**: 프로필 사진 및 추가 서류 업로드 영역에 사이트 표준 톤앤매너(Dashed Border, Light Background, List Card)를 적용하여 시각적 완성도 및 사용성 강화.
  - **전화번호 자동 포맷팅**: 등록 및 수정 시 전화번호 입력과 동시에 하이픈(`-`)이 자동으로 삽입되도록 로직을 추가하여 입력 편의성 증대 및 데이터 규격 통일.
- **운전원 목록 가시성 개선**:
  - 목록 테이블에서 불필요한 '영업넘버' 컬럼을 제거하고, 전화번호 열에 자동 하이픈 포맷팅을 적용하여 가독성 향상.

## 📅 2026-03-19: ELS 차량용 운송관리(독립형 앱) 고도화 및 인트라넷 연동
### 핵심 성과
- **독립형 운송관리 앱(`(standalone)/driver-app`) 기능 강화**:
  - **운전원 정보 자동 매칭**: 전화번호나 차량번호 입력 시 `driver_contacts` 테이블을 조회하여 **차량ID(Vehicle ID)와 이름**을 자동으로 완성하는 프로필 검색 로직 구현.
  - **상세 운송 정보 필드 추가**: `씰넘버(Seal Number)` 및 `컨테이너 타입` (기본값 40FT) 입력 칸을 신설하여 정밀한 운송 기록 지원.
  - **모바일 최적화 UI**: 스마트폰 사용 환경에 맞춰 **버튼 크기와 패딩을 대폭 확대**하고, 텍스트 가독성을 개선하여 터치 인터페이스 편의성 극대화.
  - **실시간 운행 타이머 & Media Session**: 운행 중 **초 단위 실시간 타이머**를 화면에 표시하고, Media Session API를 통해 **잠금화면 및 알림바**에서도 운행 상태와 시간을 확인할 수 있도록 개선.
  - **GPS 전송 로직 고도화**: 운행 상태(이동 30초, 일시정지 1분, 대기 2분)에 따라 전송 주기를 동적으로 조절하고, 전송 횟수 및 GPS 수신 상태를 실시간 표시.
  - **운송 히스토리 섹션**: 하단에 월별/일별 운송 기록 리스트를 추가하여 운전원이 본인의 과거 기록을 즉시 확인할 수 있도록 구현.
  - **앱 아이콘 및 PWA 리뉴얼**: 메인 사이트 로고를 응용한 **신규 차량 테마 앱 아이콘**을 제작/적용하고, 전용 PWA 매니페스트(`manifest_driver.json`) 구축.

- **Vercel 배포 빌드 오류 수정 및 Route Group 구조 안정화**:
### 핵심 성과
- **Vercel 빌드 오류 해결**:
  - **오류 원인 파악**: Route Group(`(main)`, `(standalone)`) 도입으로 최상위 루트 레이아웃(`app/layout.js`)이 제거된 상태에서, 최상위에 방치된 `not-found.js`와 `error.js`가 레이아웃 누락으로 빌드 에러를 유발함(`not-found.js doesn't have a root layout`).
  - **구조적 해결**: 최상위 `not-found.js`와 `error.js`를 삭제하고, 각각의 Route Group 내부로 이동/재생성하여 각 그룹의 전용 레이아웃을 타도록 수정.
- **경로 오류 수정 (Build Error)**:
  - Route Group 이동 후 상대 경로(`../../`)가 어긋난 `admin/page.js`, `login/page.js`, `(main)/page.js`의 임포트 경로를 **별칭(Alias, `@/`)** 방식으로 변경하여 빌드 안정성 확보.
  - `@/app/employees` 별칭이 `@/app/(main)/employees`로 변경됨에 따라 관련 파일(`webzine`) 수정.
  - `(main)/layout.js` 내 잘못된 `globals.css` 로컬 임포트 제거.
- **그룹별 전용 에러 핸들링**:
  - **(main)**: 기존 인트라넷 스타일의 404/에러 페이지 유지 (헤더/푸터 포함).
  - **(standalone)**: 운전원 앱의 심플한 화이트/블루 테마에 맞춘 독립형 404/에러 페이지 구축 (풀스크린 방식).

## 📅 2026-03-18: 기상 대시보드 고도화 및 전역 비주얼(헤더/히어로) 리뉴얼
### 핵심 성과
- **기상 대시보드 품질 향상**:
  - **아이콘 전면 교체**: 기존의 불균형했던 날씨 아이콘들을 통일감 있는 **3D 투명 PNG 세트**로 일괄 교체. 배경 제거 스크립트를 통해 어떤 배경색에서도 깨끗하게 노출되도록 처리.
  - **대기질 데이터 정합성 확보**: 하단 지점별 날씨 카드에서 미세먼지 상태가 랜덤(Mock)으로 표시되던 버그를 수정하여, 우측 상단 메인 카드와 동일하게 **실제 API 데이터(PM10)**를 기반으로 표시되도록 로직 일원화.
- **사이트 비주얼 아이덴티티 강화**:
  - **메인 히어로 최적화**: 메인 슬라이드의 이미지를 최신 카톡 전송 이미지로 교체하고, 인물과 차량이 잘 보이도록 배경 위치(`center 80%`)와 문구를 최적화.
  - **전역 헤더 이미지 통일**: 회사소개(개요, 비전, 조직도, 연혁, ESG, 사원복지) 및 문의하기 페이지의 헤더 배경을 기존의 중구난방 방식에서 최신 프리미엄 물류 이미지로 통일하여 브랜드 일관성 부여.
  - **사업 영역 이미지 교체**: '철강재 및 일반 물류' 섹션의 배경 사진을 최신 고화질 사진으로 교체.
- **아산지점 배차판 오늘 날짜 선택 로직 수정**:
  - 서버(UTC)와 클라이언트(KST) 간의 시간 차이로 인해 '오늘' 버튼 클릭 시 어제 날짜가 선택되던 문제를 한국 시간 기준으로 정확히 보정하여 실무 혼선 방지.

## 📅 2026-03-17: 구간조회 네이버 지도 기반 경로 탐색 - 터미널 목록 고도화
### 핵심 성과
- **기본 터미널 위치(좌표) 및 행정동 교정**:
  - 기존 주소 위치가 부정확했던 터미널 목록 14곳(부산북항, 부산신항, 인천항, 인천신항, 인천항국제여객, 광양항, 평택항, 울산구항, 울산신항, 포항항, 군산항, 마산항, 대산항, 의왕ICD)에 대하여 실제 지리적 위치(네이버/카카오 지도 기준)의 위경도(lat/lng)를 정밀하게 추출 및 반영.
  - 안전운임(국토부) 기점 표준 데이터 로직 호환성을 위해 `r1(시·도), r2(시·군·구)` 및 `r3(행정동)` 규칙을 엄격히 적용. 예: 광양항 -> 골약동, 의왕ICD -> 부곡동. 
- **사용자 경험 향상 (경로 검색 최적화)**:
  - 잘못 매핑되었던 태인동(광양항), 고천동(의왕ICD) 등을 골약동, 부곡동 등 올바른 행정동으로 변경함으로써, 터미널 클릭 시 정확한 안전운임 거리 계산의 기반 데이터가 자동완성 되도록 개선.
  - 마산항(가포동), 울산구항(매암동), 부산신항(성북동) 등 공공데이터 상의 동 목록(`regionsData`)에 없는 특수 매핑 동이더라도, 터미널 선택 시 드롭다운(<select>)에 누락(빈 읍면동)되지 않고 정상적으로 해당 동 이름이 표시되도록 `useMemo` 로직 보강.

---

## 📅 2026-03-12: 실적현황 대시보드 리뉴얼 (Financial & Trust Overview 구축)
### 핵심 성과
- **페이크 대시보드 제거 및 실기록 기반 전환**: 예전 임시 라이브 대시보드(Live Log) 코드를 과감히 삭제.
- **섹션 1: 매출 지표 및 팩트 시각화**: 
  - 좌측(Span 6): 2021~2024년 4개년 매출 성장 그래프를 커스텀 바 차트 및 애니메이션으로 밀도 있게 구성 (591.5억 → 766.4억).
  - 우측(Span 4): 인프라규모, 임직원수(현장직 비율 포함), 물동량 등 핵심 팩트를 3열(Rows) 그리드 카드로 빽빽하게 배치.
- **섹션 2: 인증서 & 라이선스 갤러리**:
  - 하단에 'Certifications & Licenses' 전용 7분할 그리드(Grid) 갤러리 추가.
  - 클릭 시 크게 볼 수 있는 모달(Modal) 기능 탑재, 실제 이미지 교체용 엑스박스(Placeholder) 구현.

---

## 📅 2026-03-12: 안전운임 조회 경유지 로직 정교화 및 줌 레벨 최적화
### 핵심 성과
- **경유지 거리 계산 오류 해결**: 
  - 출발지와 도착지가 동일할 때 경유지가 있음에도 0km로 표시되던 문제 수정.
  - 사용자가 직접 타이핑하지 않고 드롭다운(행정동)이나 상세주소(juso)로만 위치를 지정했을 때도 경유 거점으로 정확히 인식되도록 검증 로직 보완.
- **지도 줌 레벨(Zoom) 조정**:
  - 주소 검색 결과 선택 및 마커 클릭 시의 자동 줌 레벨을 기존 10(도시 수준)에서 14~15(상세계획/건물 수준)로 조정하여 가독성 개선.
  - 사용자의 "100% 대신 60% 수준" 피드백을 반영하여 너무 가깝거나 멀지 않은 최적의 시야 확보.

---

## 📅 2026-03-12: 중부지점 페이지 구글 스프레드시트 연동 및 여백 최적화
### 핵심 성과
- **중부지점 전용 구글 스프레드시트 임베드**: 
  - 중부지점 페이지(`/employees/branches/jungbu`)에 요청하신 외부 구글 스프레드시트 링크를 직접 연동.
  - `iframe` 임베드 방식을 사용하여 인트라넷 내에서 스프레드시트 화면이 즉시 표시되도록 구현.
- **여백 및 레이아웃 최적화**:
  - `branchMainFull` CSS 클래스를 신설하여 기존의 불필요한 패딩(40px)과 마진을 제거.
  - 상단 뉴스 티커 영역을 제외한 나머지 화면 전체를 스프레드시트가 꽉 채우도록(`calc(100vh - 44px)`) 설정하여 실무 작업 편의성 극대화.

---

## 📅 2026-03-12: 업무보고 디자인 고도화 및 편의 기능 강화
### 핵심 성과
- **디자인 일관성 확보**: 
  - 업무보고(목록, 상세, 작성/수정) 전체 페이지의 버튼 크기 및 디자인을 32px 높이로 통일하여 시각적 어색함 해소.
  - 전역 디자인 시스템과 매칭되는 패딩 및 폰트 크기 적용.
- **보고서 작성 UX 개선**:
  - **지점 자동 선택**: 보고서 작성 시 사용자의 소속 지점에 맞춰 '지점' 드롭다운이 자동으로 선택되도록 로직 보강.
  - **본문 템플릿 수정**: 하단 작성자 정보 레이블을 '성함:'에서 '이름:'으로 변경하고, 영문으로 표시되던 지점명(ID)을 '아산지점' 등 한글 명칭으로 자동 변환하여 저장되도록 개선.

---

## 📅 2026-03-12: 안전운임 조회 상하차지 동일 검색 오류 해결
### 핵심 성과
- **상하차지 동일(0km) 검색 허용**: 
  - 안전운임 구간/경로 조회 시 출발지와 도착지가 동일할 경우 네이버 지도 API가 에러를 반환하거나 프론트엔드 검증에서 차단되던 문제를 해결.
  - 동일 위치(거리 10m 이내)일 경우 API 호출을 생략하고 즉시 0km 가상 결과를 생성하도록 로직 개선.
- **안전운임 조회 API(Lookup) 안정화**:
  - 거리별 운임 조회 시 `km < 1`인 경우 400 에러를 반환하던 제약을 `km < 0`으로 변경하여 0km(또는 1km 미만) 조회 시에도 최저 거리 운임이 정상 반환되도록 수정.
  - 이를 통해 터미널 내 이동이나 동일 지역 내 조회가 중단 없이 가능해짐.

---

## 📅 2026-03-11: 엑셀 다운로드 표준 수립 및 전역 적용 (아산지점, 컨테이너 이력조회)

### 핵심 성과
- **엑셀 다운로드 표준(Standard) 수립**:
  - `docs/DESIGN_SYSTEM.md`에 엑셀 다운로드 표준 규격(Section 12) 명문화.
  - **표준 규격**: 폰트 `맑은 고딕`, 크기 `10pt`, 제목열 `옅은 회색 배경(#F2F2F2)`, `틀 고정`, `자동 필터`, `데이터 길이 기반 자동 열 너비(한글 가중치 2.1)` 적용.
- **아산지점 배차판 데이터 및 엑셀 수정**:
  - **모비스AS 데이터 복구**: '국가명' 헤더를 '국가'로 자동 매핑하여 데이터 누락 해결.
  - **엑셀 내보내기 통합**: '통합현황' 뷰에서도 엑셀 다운로드가 가능하도록 서버 측 로직 구현 및 표준 디자인 적용.
- **컨테이너 이력조회 엑셀 기능 고도화**:
  - 기존에 누락되었던 **1행 틀 고정(Freeze Panes)** 및 **자동 필터** 기능 복구.
  - 한글 가중치를 고려한 정밀한 **자동 열 너비(Auto Width)** 로직 적용으로 가독성 극대화.
  - 제목열 및 조건부 셀 스타일(수입/반입)의 폰트를 `맑은 고딕`으로 통합.

- **연락처 메뉴 개편 및 필드 확장**:
  - **명칭 변경**: '작업지안내' 메뉴를 **'고객사정보'**로 변경하여 직관성 강화.
  - **메뉴 재정렬**: 사용자 요청에 맞춰 사이드바 순서 조정(사내 → 외부 → 고객사 → 협력사 → 운전원).
  - **외부연락처 확장**: '대표 연락처' 외에 **'직통/기타 연락처(phone_2)'** 필드를 전역(DB, API, UI, 엑셀)에 추가.
  - **일괄 삭제 기능**: 엑셀 업로드 양식에 **'데이터 삭제(Y)'** 컬럼을 추가하여, ID가 있는 행에 'Y' 표기 시 일괄 삭제가 가능하도록 로직 구현.
  - **엑셀 업로드 양식 현행화**: 변경된 명칭(고객사정보) 및 추가 필드, 삭제 기능을 반영한 통합 엑셀 템플릿/업로드 로직 업데이트.
- **문의하기 페이지 UI/UX 전면 개편**:
  - **디자인 고도화**: 글래스모피즘(Glassmorphism) 테마 및 프레임워크 모션 애니메이션 적용으로 프리미엄 대시보드 스타일 구현.
  - **가독성 개선**: 불필요한 이모지/아이콘을 제거하고 텍스트 중심의 정갈한 디자인 적용.
  - **모바일 최적화**: 반응형 레이아웃 재설계 및 터치 인터페이스 최적으로 모바일 사용성 강화.

### 변경 파일
- `web/constants/intranetMenu.js` — 메뉴 명칭 및 순서 변경
- `web/supabase_sql/supabase_intranet_tables.sql` — `external_contacts` 테이블 `phone_2` 컬럼 추가
- `web/app/api/external-contacts/` (route.js, [id]/route.js) — CRUD API에 `phone_2` 반영
- `web/app/api/contacts/excel/` (template, upload) — 엑셀 양식 명칭 및 컬럼 현행화
- `web/app/employees/(intranet)/external-contacts/` (전체) — 리스트, 상세, 등록/수정 UI에 필드 추가
- `web/app/employees/(intranet)/work-sites/page.js` — 페이지 내 텍스트 명칭 변경('고객사정보')

---

## 📅 2026-03-09: 자료실(업무/서식) 다운로드 로직 일원화 및 브라우저 호환성 강화

### 핵심 성과
- **다운로드 프록시 구현 (NAS)**:
  - 그동안 NAS 파일을 브라우저에서 직접 열려고 할 때 발생하던 권한 및 브라우저 호환성 문제를 해결하기 위해, 서버 측에서 파일을 스트리밍하는 **다운로드 프록시(`api/nas/files?download=true`)** 로직 구현.
- **브라우저 호환성 및 한글 파일명 깨짐 해결**:
  - `Content-Disposition` 헤더에 RFC 5987 표준(`filename*=UTF-8''`)을 적용하여 Chrome, Edge, Safari, Firefox 등 모든 브라우저에서 한글 파일명이 정식으로 내려받아지도록 최적화.
- **자료실 UI/UX 통일 (Consistency)**:
  - 업무자료실과 서식자료실의 상세 페이지 UI를 동일한 레이아웃과 톤앤매너로 매칭.
  - 첨부파일 섹션을 기본적으로 펼쳐진 상태로 노출하여 사용자 접근성 향상.
  - S3와 NAS 저장소 위치에 상관없이 동일한 사용자 경험 제공.

### 변경 파일
- `web/app/api/nas/files/route.js` — NAS 다운로드 프록시 처리 추가
- `web/app/api/s3/files/route.js` — 한글 파일명 호환성 헤더 최적화
- `web/app/employees/(intranet)/work-docs/[id]/page.js` — 상세 페이지 UI 및 다운로드 로직 개선
- `web/app/employees/(intranet)/form-templates/[id]/page.js` — 업무자료실과 통일된 디자인 및 로직 적용

---

## 📅 2026-03-09: 자유게시판 UI/UX 고도화 및 작성자 표시 보강

### 핵심 성과
- **작성자 식별 자동화**: 
  - 게시글 작성자 ID(UUID) 대신 실명과 직급이 표시되도록 개선.
  - `profiles` 테이블과 `user_roles` 테이블을 교차 검증하여, 프로필이 없는 사용자도 권한 테이블의 이름을 참조하여 "알 수 없음" 노출 최소화.
  - 목록과 상세 페이지 모두에 **성함 + 직급** (예: 홍길동 과장) 포맷 적용.
- **상세 페이지 디자인 전면 개편 (Premium Look)**:
  - 기존의 망가진 레이아웃을 폐기하고, **프리미엄 대쉬보드 스타일**의 `detailCard` 디자인 적용.
  - 제목 폰트 강조, 메타정보(작성자, 날짜, 조회수) 아이콘화, 본문 가독성(행간 1.8) 최적화.
  - 첨부파일 영역 구분 및 다운로드 버튼 디자인 개선.
- **반응형 대응 및 인터랙션**:
  - `framer-motion`을 활용한 부드러운 진입 애니메이션 추가.
  - 모바일 환경에서 버튼이 겹치지 않도록 세로 정렬 및 크기 최적화.

### 변경 파일
- `web/app/api/board/route.js` — 목록 API 이름 조회 로직 강화
- `web/app/api/board/[id]/route.js` — 상세 API 작성자 교차 검증 로직 구현
- `web/app/employees/(intranet)/board/free/[id]/page.js` — 상세 페이지 UI 전면 재구축
- `web/app/employees/(intranet)/board/free/page.js` — 목록 페이지 직급 표시 추가
- `web/app/employees/(intranet)/board/board.module.css` — 프리미엄 디자인 클래스 추가 및 폼 스타일 보강

---

## 📅 2026-03-09: 컨테이너 이력조회 UI/UX 보강 및 현황판 업데이트

### 핵심 성과
- **컨테이너 이력조회 편의성 개선**:
  - 로그 터미널 패널을 기본적으로 펼쳐진 상태(`isLogCollapsed: false`)로 변경하여 실무 가시성 확보.
  - 서버 측 토큰 방식의 엑셀 다운로드 오류를 해결하기 위해, 브라우저에서 즉시 생성되는 **클라이언트 사이드 엑셀 다운로드(XLSX)** 로직 전면 재구축.
  - 데이터 평면화 및 최적화된 열 너비(`!cols`) 적용으로 엑셀 가독성 향상.
- **MISSION_CONTROL & DEV_LOG 동기화**: 프로젝트 누적 성과를 최신화하고 AI 통합 메모리 체계 정비.
- **안전운임 구간/경로 조회 UI/UX 전면 고도화**: (아래 실무 내역 참조)
  - 고시(2026 제정안) 기점/좌표 최적화, 행정복지센터 자동 검색/변환, 3번 줄 연동 UX 개선 등.

### 변경 파일
- `web/app/employees/container-history/page.js` — 로그 기본값 및 엑셀 다운로드 로직 수정
- `docs/MISSION_CONTROL.md` — 현황판 업데이트
- `docs/DEV_LOG.md` — 개발 이력 업데이트 (현재 파일)

---

## 📅 2026-03-09: 안전운임 구간/경로 조회 UI/UX 전면 고도화 (오전 작업)

## 📅 2026-02-27: NAS 극한 환경 안정화 (보강)
- **세션 수 강제 하향**: 동시 브라우저 세션 수를 3개에서 2개로 물리적 강제 조정 (`els_web_runner_daemon.py`, `app.py`).
- **부팅 간격 대폭 연장**: 브라우저 간 부팅 간격을 25초에서 60초로 늘려 초기 리소스 스파이크 분산.
- **실패 로그 분석 및 수정**: 브라우저 #3 실행 실패 원인이 리소스 고갈임을 확인하고, 기본 세션 수를 2개로 고정하여 안정성 확보.

| 날짜 | 내용 |
|------|------|
| 2026-03-01 | 모바일 UI 최적화, PWA 스플래시 활성화, 아산 배차판 통합현황 기본값 적용 |
| 2026-02-26 | NAS 극한 환경 안정화 로직 강화, 뉴스/날씨 모바일 S24 최적화 |
| 2026-02-25 | 컨테이너 이력조회 실시간 로그 스트리밍 및 조회 정합성 최적화 |
| 2026-02-23 | v1.0.0 클린업, 데드코드 제거, AI 통합 메모리(`docs/`) 체계 구축 |
| 2026-02-13 | 사다리 게임 모바일 레이아웃 및 동물 캐릭터 반응 시스템 추가 |
| 2026-01-19 | NAS 자료실: ZIP 다운로드, 드래그앤드롭, 다중선택 UX 개선 |
